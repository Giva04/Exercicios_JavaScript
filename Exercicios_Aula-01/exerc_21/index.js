// Declare duas variáveis de string, "nome" e "sobrenome", e atribua valores a elas. Utilize o operador de concatenação (+) para concatenar as duas variáveis e imprima o resultado no console. Ou utilize template strings.

let name = 'Giva'
let surname = 'Gomez'

let myName = name + ' ' + surname

console.log(myName)
